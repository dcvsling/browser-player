# Browser Player AI - Requirement

## 1. 專案目標
- 建立一個無後端、完全在瀏覽器執行的影片播放器。
- 影片來源為 Microsoft OneDrive，透過 OAuth / MSAL 在前端完成登入與授權。
- 專案採純 HTML / CSS / JavaScript，不使用前端框架。
- 介面語言為繁體中文，桌機體驗優先。

## 2. 技術與部署需求
- 使用 Vite 作為本機開發與正式打包工具。
- 前端程式碼以 TypeScript 為主。
- 支援 `npm run dev`、`npm run build`、`npm run preview`。
- 本機 OAuth redirect URI 使用 `http://localhost:4200/auth`。
- 路由採用 History API，不使用 `/#/`。
- 靜態部署需支援 SPA fallback；GitHub Pages 類型部署以 `404.html` 做路由還原。
- 部署流程需可直接使用 GitHub Actions 完成 build 與 GitHub Pages 部署，不依賴 Azure Static Web Apps deploy action 或 deployment token。
- 需重新支援 PWA，可安裝為 Android 手機上的應用程式。
- PWA 至少需包含 `manifest`、`service worker`、安裝能力與 app shell 快取。

## 2.1 架構要求
- 來源存取需有抽象層，避免 UI 層直接耦合 OneDrive / Local 差異。
- 來源管理需模組化（來源狀態管理與來源存取流程分離）。

## 3. 驗證與雲端整合
- 使用 MSAL Public Client + PKCE 完成登入。
- OAuth 權限至少包含 `User.Read`、`Files.Read`、`Files.Read.All`。
- 可從指定 OneDrive 資料夾讀取 mp4 清單。
- 支援完整重建與 delta 增量同步。
- 影片播放前需能補齊可播放的下載串流 URL。
- 雲端資料需快取於 `localStorage`，並保留同步時間與必要 marker。
- 進站時不得無條件強制重建雲端清單。
- 應先使用快取狀態、`folderCTag`、`deltaLink` 等條件判斷是否需要同步。
- 若因快取資料結構升級需要補 metadata，應以版本升級方式只處理必要的一次，不可每次進站都觸發完整重建。

## 4. 頁面與導覽
- 最上層至少包含以下頁籤：
- `播放器`
- `播放清單`
- `設定`
- `排程`
- URL 應對應為：
- `/player`
- `/playlist`
- `/settings`
- `/schedule`
- 站台需適應手機與窄視窗，包含一般直立手機與可摺疊裝置寬度變化。
- 介面需可適應 Android 手機，例如 Redmi Note 11 Pro 5G 與 Samsung Fold 4 類型尺寸。
- 手機頂部導覽需採緊湊配置：
- 頁面切換標籤位於上方靠左
- 帳號/登入登出位於上方靠右
- icon 與按鈕需縮小

## 5. 播放器需求
- 播放器使用原生 `video` 元件作為核心播放媒介。
- 不使用原生 `controls`，改為全自訂控制介面。
- 播放器控制需使用 Material Symbols Rounded icon，整體 theme 一致。
- 播放器需提供：
- 播放 / 暫停
- 上一部 / 下一部
- 倒退 10 秒 / 快轉 10 秒
- 進度條拖曳與點擊定位
- 音量
- 靜音
- 播放速度
- 隨機播放
- 循環模式
- 播放清單抽屜開關
- 自訂全螢幕
- 全螢幕時應由播放器容器進入全螢幕，不只 video 元件本身。
- 全螢幕時播放器 overlay 與右側播放清單抽屜必須仍可顯示。
- 全螢幕狀態下，幾秒無滑鼠移動、觸碰或操作時，控制項需自動隱藏；再次互動時再顯示。
- 播放器下方不可再出現獨立於播放器外的功能區；控制需整合在播放器內。
- 由設定搬回播放器的功能，需位於進度條下方，與主要播放器按鈕水平排列在其右側。
- 在手機上需保持可觸控操作，控制按鈕尺寸與排列不可過密。
- 播放器 overlay、抽屜與控制列需能在窄螢幕下重新排版。
- 手機 `player` 頁需以影片播放為優先，控制層避免重疊遮擋主要操作。
- 播放清單按鈕在手機上需固定可點擊，不可被其他層攔截。
- 手機橫向時，播放器播放清單抽屜應為滿高側欄，清單內容採上下捲動。

## 6. 播放清單與清單頁需求
- 清單頁需同時顯示：
- 雲端清單
- 自訂清單
- 清單頁整頁不可捲動；左右清單各自擁有獨立捲動。
- 各清單高度需隨外框剩餘空間自動延展，不可固定高度導致留白錯位。
- 雲端清單、自訂清單、播放器右側播放清單都必須支援兩種檢視模式：
- 文字模式
- 縮圖模式
- 各清單的檢視模式必須各自獨立保存，不可共用同一組設定。
- 切換檢視模式的按鈕需放在該清單標題列右側，與清單名稱同一橫列。
- 文字模式下，選項以傳統列表方式顯示。
- 縮圖模式下：
- 不必一列只顯示一個項目
- 應以多欄卡片方式排列
- 每個選項以影片縮圖盡量鋪滿整個卡片
- 片名平常不顯示，hover / focus / active 時才顯示
- 片長需顯示在卡片上
- 所有清單選項都需顯示影片長度。
- 清單需提供排序功能。
- 排序需支援遞增與遞減。
- 排序欄位至少包含：
- 名稱
- 最後更新日期
- 大小
- 排序能力至少需適用於雲端清單；若資料可用，也應能延伸到其他清單檢視。
- 手機 `playlist` 頁版面規則：
- 直向為上下各半
- 橫向為左右各半
- 兩區需有清楚分隔（邊線或底色差異）
- 手機 `playlist` 頁預設僅顯示清單名稱；工具列可按需展開，不可直接移除必要操作功能。

## 7. 清單互動規則
- 在播放清單頁：
- 雲端清單點擊時，只做「加入到目前正在編輯的自訂清單」。
- 若該影片已經存在於目前自訂清單中，點擊時不做任何事，不可觸發移除。
- 自訂清單中的選項點擊時，表示「從目前自訂清單移除」。
- 既然整列點擊已代表移除，自訂清單項目右側原本的 `-` 按鈕需移除。
- 播放器右側播放清單點擊時，表示切換播放到該項目。
- 整列點擊應作為主要操作，不再依賴每列中的次要文字按鈕。

## 8. 自訂清單需求
- 使用者可建立多個自訂清單。
- 建立自訂清單時，空白名稱使用預設名稱 `我的清單`；若預設名稱已存在，需提示使用者輸入清單名稱。
- 自訂清單名稱僅允許中文、英文、數字、空格、底線與連字號。
- 可切換目前正在編輯的自訂清單。
- 可刪除自訂清單。
- 可指定某個自訂清單作為播放器的播放來源。
- 自訂清單內容與選取狀態需保存，下次開啟可還原。

## 9. 設定需求
- 站台需提供獨立的 `設定` 頁面。
- 設定頁負責集中管理站台預設設定與快捷鍵設定。
- 所有設定需保存於 `localStorage`，下次進站時自動還原。
- 目前設定頁至少包含：
- 快捷鍵啟用開關
- 快捷鍵編輯
- 快捷鍵重設

## 9.1 背景排程需求
- 站台需提供獨立的 `排程` 頁面，顯示背景工作進度。
- 排程頁需顯示等待、處理中、完成、失敗數量。
- 排程頁的等待、處理中、完成、失敗需為篩選按鈕；點選後只顯示該狀態，再次點同一按鈕則回到全部。
- 排程列表中的失敗項目被點選時，需改回等待中並重新嘗試。
- 排程頁需提供「一次要處理多少項目」輸入框與更新確認按鈕。
- 本機來源匯入時，需先載入檔案資訊與片長，不可同步等待 ffmpeg.wasm 擷取縮圖。
- 本機來源匯入不可等待大量檔案寫入 IndexedDB；匯入完成後需先能直接播放，檔案存取持久化改由背景排程處理。
- 本機縮圖需進入背景排程，以 ffmpeg.wasm 擷取後保存，關閉應用程式後仍可還原。
- 雲端縮圖下載排程執行時，需重新透過 Graph API 取得最新 thumbnails URL，不可依賴舊快取 URL。
- 若縮圖已成功保存，後續同步或重新開啟時需直接顯示上次成功保存的截圖，不應再次加入縮圖排程。
- 已知排程類型至少包含：
- ffmpeg.wasm 擷取本機影片縮圖
- 下載雲端已提供的縮圖

## 10. 快捷鍵需求
- 快捷鍵需可由使用者自行修改。
- 支援單鍵與組合鍵。
- 組合鍵至少支援 `Ctrl + Alt + Key` 類型。
- 支援特殊鍵，如：
- ArrowUp / ArrowDown / ArrowLeft / ArrowRight
- Home / End
- PageUp / PageDown
- Enter / Escape / Tab / Delete / Backspace
- 快捷鍵設定需至少涵蓋：
- 播放 / 暫停
- 倒退 10 秒
- 快轉 10 秒
- 上一部
- 下一部
- 靜音
- 隨機
- 循環
- 播放清單抽屜
- 全螢幕
- 上方頁籤切換：
- `F1`：播放器
- `F2`：播放清單
- `F3`：設定
- `F4`：排程
- 音量增加 / 音量減少
- 播放速度增加 / 播放速度減少
- 音量與播放速度快捷鍵預設為未設定。
- 調整音量或播放速度後，快捷鍵不應因 focus 停留在控制項上而失效。
- 除滑鼠快捷鍵設定擷取外，整個應用程式的滑鼠右鍵不應展開選項或觸發功能。
- 若滑鼠右鍵已設定為快捷動作，右鍵需執行該動作但仍不可展開瀏覽器選單。
- 滑鼠快捷鍵設定需支援滑鼠按鍵與特殊功能鍵。

## 11. 帳號顯示需求
- 右上角帳號區應顯示使用者名稱或頭像資訊。
- 後續調整為僅保留頭像 badge，不常駐顯示名稱文字。
- 名稱可保留於 title 或無障礙文字中。

## 12. 狀態保存需求
- 使用 `localStorage` 保存以下資訊：
- 播放偏好
- 快捷鍵設定
- 自訂清單內容
- 目前編輯中的自訂清單
- 目前播放器使用的清單來源
- 播放位置
- 雲端影片快取
- 檢視模式設定
- 清單排序設定
- 雲端快取版本資訊

## 13. 快取與舊版清理需求
- 若站台從舊 PWA / 舊 service worker 架構遷移，需在新版啟動時嘗試清理舊的離線狀態。
- 清理內容至少包含：
- 舊 service worker 註銷
- 舊 cache storage 清理
- 清理範圍應盡量限制在本應用 scope，避免波及同網域其他應用。

## 14. 驗收重點
- 可在無後端情境下登入並讀取 OneDrive 影片。
- 可正確播放、切換、快轉、快退、調整音量、速度、循環與隨機。
- 全螢幕下自訂控制與播放清單抽屜仍可使用。
- 清單頁不會整頁捲動，左右清單各自捲動。
- 自訂清單與雲端清單的點擊語意明確且互不混淆。
- 各清單檢視模式可各自切換並被保存。
- 各清單排序設定可各自切換並被保存。
- 舊 PWA / service worker 使用者在進入新版站台後，應能逐步脫離舊快取影響。
- 重新整理或重新進站後，偏好、清單與播放狀態可恢復。
- Android 手機上可安裝為 PWA，並可獨立視窗啟動。
- 排程頁可顯示本機縮圖擷取與雲端縮圖下載進度。
- 本機縮圖在重新開啟應用程式後仍可顯示。
- 手機直立、橫向、窄寬度與可摺疊寬度變化下，版面仍可正常操作。
- `player` 頁主標題與 `document.title` 應顯示等待播放或正在播放中的影片名稱。
- `playlist` 頁不需顯示正在播放檔名，應顯示固定頁面標題。
